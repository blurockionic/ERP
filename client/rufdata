

<div> jai shree ram </div>

<div className="flex flex-col items-start mt-2 rounded ">
  <label className="flex items-center mr-4">
    <input
      type="radio"
      name="status"
      value="onhold"
      checked={order.status === "onhold"}
    
      onChange={() => handleStatusChange(order, "onhold")}
      className="mr-2"
    />
    <span className="font-medium">On Hold</span>
  </label>
  <label className="flex items-center">
    <input
      type="radio"
      name="status"
      value="scrap"
      checked={order.status === "scrap"}
      onChange={() => handleStatusChange(order, "scrap")}
      className="mr-2"
    />
    <span className="font-medium">Scrap</span>
  </label>
  <label className="flex items-center">
    <input
      type="radio"
      name="status"
      value="completed"
      checked={order.status === "completed"}
      onChange={() => handleStatusChange(order, "completed")}
      className="mr-2"
    />
    <span className="font-medium">Completed</span>
  </label>
  <label className="flex items-center">
    <input
      type="radio"
      name="status"
      value="noresponse"
      checked={order.status === "noresponse"}
      onChange={() => handleStatusChange(order, "noresponse")}
      className="mr-2"
    />
    <span className="font-medium">No Response</span>
  </label>
</div>